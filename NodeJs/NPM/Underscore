#underscore는 빈약한 자바스크립트의 배열관련 함수를 보충하는데 쓰인다.

var _ = require('underscore');
var arr = [3,6,9,1,12];
console.log(arr[0]);  //3
console.log(_.first(arr)); //3
console.log(arr[arr.length-1]); //12
console.log(_.last(arr)); //12
